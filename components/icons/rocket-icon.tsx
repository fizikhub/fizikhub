"use client";

import { CustomRocketIcon } from "@/components/ui/custom-rocket-icon";

export function RocketIcon({ className }: { className?: string }) {
    return <CustomRocketIcon className={className} />;
}
